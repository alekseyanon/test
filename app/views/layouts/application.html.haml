!!! 5
%html
  %head
    = csrf_meta_tag
    %meta(charset="utf-8")
    %meta(content="IE=edge,chrome=1" http-equiv="X-UA-Compatible")
    %title(Smorodina)
    = stylesheet_link_tag    "application", :media => "all" 
    = javascript_include_tag "application" 
    = csrf_meta_tags 

  %body
    #user_nav.btn-group
      
      %br/
      -# comment 1
      -# = link_to "Отправить почту", sendmail_path
      .btn= link_to "Личный кабинет", profile_path(:type => 'traveler') 
      .btn= link_to "Забыли пароль", forget_password_path 

      - if current_user
        .btn= link_to "Редактировать профиль", edit_user_path(current_user)
        .btn= link_to "Настройки", settings_user_path(current_user)

        .btn= link_to "Log out", logout_path, :method => :delete 
        = current_user.try(:email) 
        = current_user.try(:state)
        %p role: 
        -if current_user.role?(:traveler)
          %p role traveler
        = current_user.roles  
      - else
        -# = link_to "Log in", login_path 
        -# .btn= link_to "Register", signup_path(:type => 'traveler')

        -# .btn= link_to "login ", login_path(:type => 'traveler')
    = yield 