!!! 5
%html
  %head
    = csrf_meta_tag
    %meta(charset="utf-8")
    %meta(content="IE=edge,chrome=1" http-equiv="X-UA-Compatible")
    %title(Smorodina)
    = stylesheet_link_tag    "application", :media => "all" 
    = javascript_include_tag "application" 
    = csrf_meta_tags 

  %body
    #user_nav.btn-group
      
      %br
      .btn=link_to "домой", root_path
      .btn= link_to "Личный кабинет", profile_path(:type => 'traveler') 
      .btn= link_to "Забыли пароль", forget_password_path 

      - if current_user
        .btn= link_to "Редактировать профиль", edit_user_path(current_user)
        .btn= link_to "Настройки", settings_user_path(current_user)
        .btn= link_to "Log out", logout_path, :method => :delete 
      .btn=link_to "список объектов", landmark_descriptions_path
      .btn=link_to "поиск объектов", search_landmark_descriptions_path
    = yield 