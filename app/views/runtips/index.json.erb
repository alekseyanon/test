[
  <% @geo_object.runtips.each do |runtip| %>
    {
      id: <%= runtip.id %>,
      body: <%= runtip.body %>,
      created: <%= Russian::strftime(runtip.created_at, "%d %B %Y") %>,
      user: <%= runtip.user.to_json.html_safe %>
    },
  <% end %>
]
